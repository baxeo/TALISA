import React from "react";
import ReactDOM from "react-dom/client";
import SupermarketDashboard from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SupermarketDashboard />
  </React.StrictMode>
);
